export function capitalizeFirstLetter(string) {
    return string.charAt(0).toUpperCase() + string.slice(1);
}

// export class quiz grading function
// take the correct answers and user answers as arguments
// compare user answers to correct answers 
// for each correct answer add 2 points to results array
// for each incorrect answer subtract 1 point from results array
// once the arrays are empty return results array

// export sorting quiz grading function